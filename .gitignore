# .NET
bin/
obj/
*.user
.vs/

# Node / Angular
node_modules/
dist/
# The GitHub Action ships its bundled dist (node20 action contract)
!action/dist/
.angular/
.nx/
out-tsc/
# Test-coverage output only — never the Coverage/ project folder (git ignores case on Windows)
ClientApp/coverage/

# Environment
.env
*.pem

# Local smoke-test logs
boot-*.log

# Playwright MCP browser-session artifacts
.playwright-mcp/
